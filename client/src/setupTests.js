// Import necessary utilities for testing
import "@testing-library/jest-dom"; // Adds custom matchers for asserting on DOM nodes

// Extend Jest with custom matchers from React Testing Library
// Example: expect(element).toBeInTheDocument()
